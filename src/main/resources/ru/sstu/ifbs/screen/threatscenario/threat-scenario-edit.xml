<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<window xmlns="http://jmix.io/schema/ui/window"
        caption="msg://threatScenarioEdit.caption">
    <data>
        <instance id="threatScenarioDc" class="ru.sstu.ifbs.entity.ThreatScenario">
            <fetchPlan extends="_base">
                <property name="threat" fetchPlan="_base"/>
                <property name="tactics" fetchPlan="_local">
                    <property name="value" fetchPlan="_local">
                        <property name="techniques" fetchPlan="_local"/>
                    </property>
                    <property name="techniques" fetchPlan="_base"/>
                </property>
            </fetchPlan>
            <loader/>
        </instance>
        <collection id="tacticsDc" class="ru.sstu.ifbs.entity.Tactic">
            <fetchPlan extends="_local">
                <property name="techniques" fetchPlan="_local"/>
            </fetchPlan>
        </collection>
    </data>
    <facets>
        <dataLoadCoordinator auto="true"/>
        <screenSettings id="settingsFacet" auto="true"/>
    </facets>
    <actions>
        <action id="windowCommitAndClose" caption="msg:///actions.Ok"
                icon="EDITOR_OK"
                primary="true"
                shortcut="${COMMIT_SHORTCUT}"/>
        <action id="windowClose"
                caption="msg:///actions.Close"
                icon="EDITOR_CANCEL"/>
    </actions>
    <dialogMode height="600"
                width="800"/>
    <layout spacing="true" expand="scrollBox">
        <scrollBox id="scrollBox" spacing="true">
            <buttonsPanel>
                <popupButton id="addTacticBtn" caption="addTacticBtn.caption" icon="ADD_ACTION"/>
            </buttonsPanel>
            <hbox id="tacticsBox"/>
        </scrollBox>
        <hbox id="editActions" spacing="true">
            <button id="commitAndCloseBtn" action="windowCommitAndClose"/>
            <button id="closeBtn" action="windowClose"/>
        </hbox>
    </layout>
</window>
